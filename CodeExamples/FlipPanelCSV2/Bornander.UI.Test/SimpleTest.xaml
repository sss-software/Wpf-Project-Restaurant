<Window x:Class="Bornander.UI.Test.SimpleTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:f="clr-namespace:Bornander.UI;assembly=Bornander.UI"
        WindowStartupLocation="CenterScreen"
        Title="SimpleTest" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <f:FlipPanel x:Name="flipper" Grid.Row="0">
            <f:FlipPanel.Front>
                <Border Background="Red" BorderBrush="Black" BorderThickness="2">
                    <Button Content="Front" VerticalAlignment="Center" Click="Button_Click"/>
                </Border>
            </f:FlipPanel.Front>
            <f:FlipPanel.Back>
                <Border Background="Green" BorderBrush="Black" BorderThickness="2">
                    <Button Content="Back" VerticalAlignment="Center" Click="Button_Click"/>
                </Border>
            </f:FlipPanel.Back>
        </f:FlipPanel>
        <StackPanel Grid.Row="1">
            <Slider Minimum="0.2" Maximum="5.0" Value="1" ToolTip="Spin time (s)" ValueChanged="Slider_ValueChanged"/>
            <Button x:Name="flipOrientationButton" Content="Horizontal Axis" Click="Button_Click_1"/>
        </StackPanel>
    </Grid>
</Window>
