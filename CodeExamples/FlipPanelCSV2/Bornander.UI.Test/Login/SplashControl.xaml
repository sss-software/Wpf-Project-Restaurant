<UserControl x:Class="Bornander.UI.Test.Login.SplashControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="320">
    <Border BorderBrush="Black" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Source="pack://application:,,,/Bornander.UI.Test;component/Images/Splash.png" Grid.RowSpan="3"/>
        
            <ProgressBar Grid.Row="2" Height="23" Margin="5" Minimum="0" Maximum="1" Value="{Binding Path=Progress, Mode=OneWay}" />
            <Label Grid.Row="1" Content="Loading..." Foreground="Wheat" Margin="5,0,5,0"/>
        </Grid>
    </Border>
</UserControl>
