<UserControl x:Class="Wpf.Manager.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:f="clr-namespace:Bornander.UI;assembly=Bornander.UI"
             xmlns:l="clr-namespace:Wpf.Manager"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <f:FlipPanel x:Name="flipper" FrontVisible="True" SpinAxis="Vertical" Background="Gray" PreviewMouseLeftButtonDown="FlipPanel_PreviewMouseLeftButtonDown">
            <f:FlipPanel.Front>
                <l:TableFrontPanel DataContext="{Binding}"/>
            </f:FlipPanel.Front>
            <f:FlipPanel.Back>
                <l:TableBackPanel DataContext="{Binding}"/>
            </f:FlipPanel.Back>
        </f:FlipPanel>
    </Grid>
</UserControl>
